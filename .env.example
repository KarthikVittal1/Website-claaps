# Admin Authentication (required for /admin access)
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=

# Azure Storage (optional for cloud consultation persistence, falls back to local data/ directory)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=consultation-data
